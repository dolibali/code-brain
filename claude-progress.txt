Session: 2026-04-24

What was implemented
- Refactored the biggest architectural hotspots without changing the thin-service behavior.
- Split the CLI into modular command files under `src/cli/commands/` with shared helpers in `src/cli/helpers.ts`.
  `src/cli.ts` is now just a thin compatibility export to `src/cli/index.ts`.
- Split page storage responsibilities:
  - `src/pages/repository.ts` now acts as the coordinator
  - `src/pages/page-filesystem.ts` owns Markdown file walking/parsing/writes
  - `src/pages/page-indexer.ts` owns SQLite index synchronization
  - `src/pages/page-project.ts` owns project resolution helpers
  - `src/pages/types.ts` defines DTOs and the `PageService` interface
- Split MCP registration:
  - `src/mcp/server.ts` now only creates and serves the MCP server
  - `src/mcp/tools/*.ts` register individual tools
  - `src/mcp/helpers.ts` centralizes result/error handling
- Split storage migration logic:
  - `src/storage/index-db.ts` now only opens the DB and delegates to `runMigrations`
  - `src/storage/migrator.ts` introduces a lightweight migration runner with `schema_migrations`
  - `src/storage/migrations/00x-*.ts` holds explicit migration steps
- Added lightweight interface/type layers for `pages`, `links`, and `search`, and updated `open-service.ts` to expose interface-shaped service ports instead of concrete class types.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes

Session: 2026-05-01

What was implemented
- Moved remote server token generation into `braincode setup`.
- When remote server mode is configured and `server.auth_token_env` is not set in the current environment, setup now prints a strong random `export BRAINCODE_SERVER_TOKEN="..."` hint.
- Kept secrets out of YAML: setup still writes only env var names, not token values.
- Kept `braincode serve --remote` as a pure startup command; it does not silently generate transient tokens.
- Kept remote client token hints as placeholders because `BRAINCODE_REMOTE_TOKEN` must match the remote server token.
- Updated README and DESIGN.md to document the setup-time token behavior.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes

Important notes
- Package version remains `0.1.0`.
- No MCP tools or CLI command names changed.

Session: 2026-05-01

What was implemented
- Lowered the published runtime requirement from Node.js `>=24.0.0` to `>=22.20.0`.
- Changed the development Node type package from `@types/node@24` to the latest available Node 22 type line, `@types/node@22.19.17`.
- Updated `package-lock.json` so npm metadata matches the new runtime baseline.
- Updated README install notes to state that BrainCode requires Node.js `>=22.20.0`.
- Clarified that the current SQLite backend still uses Node's built-in `node:sqlite`, but this baseline avoids needing an extra SQLite flag.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm pack --dry-run` passes and reports `braincode@0.1.0`

Important notes
- Package version remains `0.1.0`.
- `npm view node@22.20.0 version` confirms the requested Node runtime version exists.

Session: 2026-05-01

What was implemented
- Simplified the human-facing CLI command surface to the new BrainCode naming:
  - `project add --name/--path/--url/--branch`
  - `pj add -n/-p/-u/-b`
  - `project list` and `pj ls`
  - `search`/`s`, `list`/`ls`, `reindex`/`idx`
- Removed the old `project register` command and old project flags (`--id`, `--root`, `--remote`, `--main-branch`).
- Removed the old `serve-http` command; remote HTTP mode now uses `braincode serve --remote` or `braincode serve -r`.
- Renamed CLI flags to the shorter convention:
  - `--context-path` -> `--context`
  - `--types` -> `--type`
  - `--tags` -> `--tag`
  - `--scope-ref` -> `--scope`
  - `--relation` -> `--rel`
  - `--full` -> `--all`
- Changed `links` to accept the slug as a positional argument instead of `--slug`.
- Updated README, DESIGN.md, integration docs, feature tracking, smoke script, and CLI behavior tests.
- Removed the README `Publish` section per request.
- Added a `clean` build step so stale `dist` files such as the removed `serve-http` command cannot leak into npm packages.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- `npm pack --dry-run` passes and no longer includes `dist/src/cli/commands/serve-http.*`

Important notes
- This intentionally breaks old CLI compatibility, matching the requested direct replacement.
- MCP tool names and behavior are unchanged.

Session: 2026-05-01

What was implemented
- Decoupled stable project identity from local machine paths.
- Treat `project id/name + git_remotes + main_branch` as the cross-machine identity.
- Treat `roots` as local mounts only; config now allows remote projects with `roots: []`.
- Added Git remote normalization and duplicate detection so different project names cannot register the same repository URL.
- Changed repeated `project add` for the same project to merge roots/remotes instead of replacing them.
- Preserved an existing project `main_branch` when adding a new local path without explicitly passing `--branch`.
- Added `git_remotes` to sync manifest project metadata.
- Added `PUT /sync/project` for remote project metadata sync; remote-created projects use `roots: []`.
- Updated `sync push` to sync project metadata before uploading pages.
- Updated `sync pull` to import remote project metadata before downloading pages.
- Documented that shared Markdown, `scope_refs`, and sync metadata should not contain local absolute paths.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- `npm pack --dry-run` passes
- `package.json` version remains `0.1.0`

Important notes
- Remote sync now rejects same normalized Git remote under different project ids with HTTP 409.
- MCP tool names and CLI command names are unchanged.

Session: 2026-05-01

What was implemented
- Added `braincode setup` as the recommended first-run and reconfiguration entrypoint.
- Added non-interactive setup for scripted installs with project registration, optional LLM, optional embedding, remote client/server config, and MCP snippets.
- Added `braincode doctor` for config, path, project root, environment variable, and remote-server readiness diagnostics.
- Added `braincode config path/show/validate/edit` for fixed config maintenance workflows.
- Added provider presets for DeepSeek, Qwen Bailian, GLM/Zhipu, MiniMax, Kimi, and custom OpenAI-compatible setup.
- Kept secrets out of config: setup only writes env var names and prints export hints for missing values.
- Kept `braincode init` as the minimal script-friendly bootstrap path.
- Updated README, DESIGN.md, and feature tracking with setup/doctor/config usage.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- `npm pack --dry-run` passes
- `package.json` version remains `0.1.0`

Important notes
- No MCP tools were added or renamed.
- The setup flow uses Node's built-in readline and adds no runtime dependency.

Session: 2026-05-01

What was implemented
- Reset package version metadata from `0.2.1` to `0.1.0`.
- Updated default MCP config version examples to `0.1.0`.
- Added README instructions for upgrading an existing global install with `npm install -g braincode@latest`.
- Added README publish steps:
  - `npm run typecheck`
  - `npm test`
  - `npm run build`
  - `npm pack --dry-run`
  - `npm login`
  - `npm publish --access public`
- Documented npm version immutability: if `braincode@0.1.0` already exists on npm, publish `0.1.1` or another new version instead.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- `npm pack --dry-run` passes and reports `braincode@0.1.0`
- No command surface or MCP tool names changed during the refactor

Important notes
- This was an internal architecture cleanup; external behavior should remain the same.
- `feature_list.json` remains fully passing and was not changed.
- The codebase now has clearer seams for future work on storage, MCP tools, and CLI commands.

What should be worked on next
- Decide whether to continue the light abstraction pass by moving `search` and `links` SQL helpers into their own modules, or stop here.
- Revisit whether test co-location is worth doing after a few more modules grow.
- Continue evaluating whether `node:sqlite` should eventually be replaced for production stability.

Session: 2026-04-30

What was implemented
- Added single-user remote deployment mode while keeping local stdio MCP unchanged.
- Added `braincode serve-http` for a single HTTP service that exposes Streamable HTTP MCP at `/mcp` and sync endpoints under `/sync/*`.
- Added Bearer token enforcement for `/mcp` and `/sync/*`, with non-localhost HTTP serving requiring `server.auth_token_env` to point at a non-empty environment variable.
- Added manual sync commands:
  - `braincode sync status`
  - `braincode sync pull`
  - `braincode sync push`
- Added config sections for `server`, `remote`, and `sync`.
- Added manifest/hash based sync:
  - `pull` downloads only changed or missing remote pages and prunes local pages missing from remote when configured
  - `push` uploads local pages whose hash differs from remote and intentionally overwrites the remote page for the same slug
- Added gzip support for JSON responses and gzip-compressed sync uploads.
- Added HTTP/sync tests covering token rejection, HTTP MCP tool discovery, remote pull, and local-overwrites-remote push.
- Updated README and DESIGN.md with remote deployment and manual sync usage.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- Existing stdio MCP behavior remains covered by the existing smoke test

Important notes
- Remote mode is single-user only; no user accounts, permissions, or merge conflict resolution were added.
- Remote server is the truth source by product convention; local sync cache can be pruned by `sync pull`.
- `sync push` follows the requested local-overwrites-remote behavior.
- Transport encryption is expected from HTTPS/TLS outside the app; server-side Markdown and SQLite remain plaintext.

Session: 2026-04-30

What was implemented
- Changed the recommended remote server command from `braincode serve-http` to `braincode serve --remote`.
- Added short flags for remote serving:
  - `-r, --remote`
  - `-i, --ip`
  - `-p, --port`
- Kept `braincode serve-http` as a compatibility alias for older scripts and docs.
- Updated README and DESIGN.md to recommend the all-long form `braincode serve --remote --ip 127.0.0.1 --port 7331`, with `braincode serve -r -i 127.0.0.1 -p 7331` as the short equivalent.
- Added CLI help coverage for the new remote serve flags.
- Clarified that `braincode serve --remote` without `-i/-p` uses `server.host/server.port`, defaulting to `127.0.0.1:7331`.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
