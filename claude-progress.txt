Session: 2026-04-19

What was set up
- Refactored the implementation from a thick service into the thin-service model defined in `docs/DESIGN.md`.
- Removed `record_change`, ingest dedupe, and service-side knowledge extraction from the runtime, CLI, MCP server, and tests.
- Reworked config loading around `roots`, `main_branch`, search-only LLM routing, and MCP metadata.
- Rebuilt page storage around full Markdown `put_page`, full slugs such as `issue/foo` and `change/2026/2026-04-18-bar`, and strict frontmatter validation.
- Updated search results to keep long-lived pages ahead of `change` pages and to surface `relatedChanges` from explicit links.
- Renamed the CLI command surface to `codebrain` and aligned README/examples with the thin-service workflow.
- Replaced the old tests with thin-service coverage for config, page parsing, page repository, search, CLI, and MCP parity.

Current status
- 24 features defined
- 24/24 features passing
- `npm run typecheck` passes
- `npm test` passes

What should be worked on next
- Consider whether to replace Node's experimental `node:sqlite` API with a stable production driver.
- Add a real search-side LLM adapter if you want query expansion or reranking beyond the current hook-based fallback path.
- Decide whether to generate a dedicated `docs/BRAIN_SYNC_RECIPE.md` companion doc from the DESIGN spec.
- Start dogfooding the thin-service workflow against a real project and collect page-quality feedback from Agents.

Notes
- `main_branch` is now a recipe-level recommendation for Agents, not a hidden server-side write restriction.
- The service boundary is intentionally narrow: `search / get_page / list_pages / put_page / link_pages / get_links / reindex`.
- `feature_list.json` still reflects the long-running harness state and currently remains fully passing after the refactor.
