Session: 2026-04-24

What was implemented
- Continued the release-hardening pass after the thin-service + model-adapter work.
- Changed `codebrain init --config <path>` to behave as a portable setup:
  when the config path is explicit and non-default, generated `brain` and `state/index.sqlite` paths are written relative to that config file instead of silently pointing at `~/.code-brain`.
- Updated config loading so missing custom config files resolve to config-relative runtime paths, while the standard default path still keeps home-based defaults.
- Added a dedicated stdio MCP smoke script at `scripts/smoke-mcp.mjs`.
  It bootstraps a temp config, writes a sample page, connects to `codebrain serve` over MCP stdio, and verifies `search` + `get_page`.
- Updated README to document portable `init --config` behavior and the new smoke-test workflow.

Current status
- `npm run typecheck` passes
- `npm test` passes
- `npm run build` passes
- `npm run smoke:mcp` passes
- Total automated tests: 31 passing

Important notes
- Default config path still uses `~/.code-brain`.
- Explicit config paths now default to `./brain` and `./state/index.sqlite` relative to the config file.
- Search is still async internally because LLM expansion/reranking and embedding lookup can be remote operations.
- The package still uses Node's built-in `node:sqlite`, so experimental warnings may appear depending on the Node version.

What should be worked on next
- Consider replacing `node:sqlite` with a stable production driver if install/runtime stability becomes a blocker.
- Add real provider integration smoke tests against one OpenAI-compatible vendor if you want higher confidence beyond mocks.
- Consider whether to add one more smoke path that validates the published tarball can run `smoke:mcp`-equivalent checks after install.
